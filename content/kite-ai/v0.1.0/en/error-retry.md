# Retry button in error bar

Network blips and provider hiccups happen. When a request fails, Kite now shows the error in a bar at the bottom of the chat with a "Retry" button right next to it.

Tap Retry and the same prompt is sent again with the same model and attachments — no need to retype anything. If the failure was transient, you usually get a clean reply on the second try.
