# Restore model on conversation load

Each conversation now remembers the model it was using. When you reopen an old chat, the model picker switches back to that model automatically, so your next message goes to the same brain that wrote the rest of the thread.

This keeps continuity intact — answers stay consistent in tone and capability — and saves you from accidentally sending a follow-up to a different (and possibly weaker) model.
