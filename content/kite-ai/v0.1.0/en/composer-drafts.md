# Per-conversation composer drafts

Half-typed a long prompt and got interrupted? Switch to another conversation, restart the app, even reboot the phone — when you come back, the draft is still in the composer where you left it.

Each conversation keeps its own draft, so jumping around does not destroy work in progress. Sending the message clears the draft for that chat; deleting the text clears it manually.
