# Export all data as JSON

Open Settings and pick "Export data" to download a single JSON file containing every conversation, project, saved prompt, tag, and setting in your Kite installation.

The file lives on your device — share it to iCloud, Dropbox, your laptop, anywhere you want a backup. Because it is plain JSON, it is also the basis for moving data between devices using the matching import feature.
