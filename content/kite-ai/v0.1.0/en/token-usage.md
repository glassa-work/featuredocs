# Token usage badge

Each model reply now shows a small badge with how many tokens it used, broken down into prompt and completion. Tap the badge to see the exact numbers.

This is useful if you are watching your spend or trying to keep prompts compact. Models charge per token, so the badge gives you a quick sense of whether your conversation is getting expensive before you hit the end of the month.
