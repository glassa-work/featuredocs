# Per-message timestamp on tap

To keep the chat view clean, timestamps are hidden by default. Tap any message bubble — yours or the model's — and a small timestamp appears underneath, showing the date and time it was sent.

Tap it again to hide it. This is handy for figuring out when a conversation actually happened, or for spotting how long the model took to respond, without cluttering the chat with timestamps you rarely need.
