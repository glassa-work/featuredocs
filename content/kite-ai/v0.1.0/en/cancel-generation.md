# Cancel in-flight generation

Sometimes you can already see the answer is heading the wrong way, or you only needed the first paragraph. While a reply is streaming, the send button turns into a stop button — tap it to cancel right away.

Whatever the model has already produced is kept in the conversation, so you can quote or build on the partial answer instead of throwing it away. Cancelling also frees up tokens you would have otherwise paid for.
